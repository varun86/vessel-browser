export const STATUS_MESSAGE_CLEAR_MS = 3000;
export const STATUS_MESSAGE_LONG_CLEAR_MS = 5000;

/** Max characters shown for tool arguments and highlight previews before truncation. */
export const MAX_PREVIEW_TEXT = 60;
/** Characters kept when truncating (leaves room for "..." ellipsis). */
export const TRUNCATE_KEEP = 57;
