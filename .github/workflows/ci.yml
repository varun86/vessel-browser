name: CI

on:
  pull_request:
  push:
    branches-ignore:
      - main
      - dev
    tags-ignore:
      - "v*"
  workflow_dispatch:

jobs:
  checks:
    runs-on: ubuntu-latest

    steps:
      - name: Check out repository
        uses: actions/checkout@v6

      - name: Set up Node.js
        uses: actions/setup-node@v6
        with:
          node-version-file: .node-version
          cache: npm

      - name: Set up npm
        run: corepack prepare npm@11.16.0 --activate

      - name: Install dependencies
        run: corepack npm ci

      - name: Audit production dependencies
        run: corepack npm run audit:prod

      - name: Audit all dependencies
        run: corepack npm run audit:all

      - name: Typecheck
        run: corepack npm run typecheck

      - name: Unit tests
        run: corepack npm run test:coverage

      - name: Build
        run: corepack npm run build

      - name: Smoke test
        run: xvfb-run -a corepack npm run smoke:test
